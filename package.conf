name="pastebin"
description="Pastebin Client - Code and text sharing utility"
version="0.1.0"
method="stow"
collection="apps"
fold=false

# Ignore documentation files
supported_os=("linux")
depends=()
requires_commands=("curl")
ignore=(
    "README.md"
)
